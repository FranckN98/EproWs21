/**
 * This Package contains all Data-Transfer-Objects (DTOs) which are a direct mapping of the entities in the {@link de.thbingen.epro.model.entity}
 * package. The DTOs also do Validation, so that no invalid Values can be posted.
 * <p>
 * The only subpackage, {@link de.thbingen.epro.model.dto.dashboard} contains the DTOs which are not a direct mapping
 * of any entity and are merely used to be able to represent all data needed for the dashboard.
 */
package de.thbingen.epro.model.dto;